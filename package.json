{
  "name": "omnistore-multi-tenant",
  "version": "1.0.0",
  "private": true,
  "description": "DigiTronics/OmniStore ERP — one command runs the whole application: Express backend API + static frontend (see DEPLOYMENT.md)",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "postinstall": "cd backend && npm install",
    "start": "node backend/server.js",
    "check": "node backend/scripts/checkEnv.js",
    "test": "cd backend && npm test",
    "test:e2e": "node tests/e2e/verify.js"
  }
}
