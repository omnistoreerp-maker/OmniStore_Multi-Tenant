# Backend Coverage Report — Phase 19B (final)

Generated by `npm run test:coverage` (Jest native `--coverage`).
All 236 tests execute the Express app **in-process** — real execution
numbers, no mocks, no exclusions.

## Totals

| Metric     | 19A.1 (before) | 19B (after) | Delta  |
|------------|----------------|-------------|--------|
| Statements | 40.62%         | **77.01%**  | +36.39 |
| Branches   | 25.83%         | **59.73%**  | +33.90 |
| Functions  | 38.53%         | **88.30%**  | +49.77 |
| Lines      | 45.25%         | **82.50%**  | +37.25 |

Target was Statements >= 80%. The run finished at **77.01%** — the
target was not met, and nothing was manipulated, mocked or excluded to
reach it. Lines (82.50%) and Functions (88.30%) exceed 80%.

## Fully or near-fully covered

- All 15 route files — 100%
- `config/index.js` — 100%
- Middleware layer — 95.27% (`auth.js` 100%, `authorize.js` 100%,
  `security.js` 96%, `errorHandler.js` 93%, `validate.js` 91%)
- Utils — 88.57% (`jwt.js` 100%, `tokenStore.js` 100%,
  `apiResponse.js` 100%, `password.js` 87.5%, `fileStore.js` 80%)
- Endpoint-tested services — sales 84.7%, purchase 84.5%, reports
  82.2%, inventory 81.4%, dashboard 79.7%, voucher 78.7%, users 75.7%,
  partners 74.4%, inventoryTransactions 73.8%

## Remaining uncovered (why the statement target is missed)

1. **Controller 500-handlers** — every controller sits at ~67–70%
   because only its `catch → 500` branches are uncovered. Covering them
   requires forcing persistence failures, i.e. mocking — disallowed.
2. **Service filter/sort branches** — customers 69%, suppliers 67%,
   treasury 62.5%, employees 68%: the uncovered statements are mostly
   optional query-filter combinations and `Failed to persist` returns.
3. **`server.js` startup block** (lines 86–99) — the `require.main`
   guard correctly never runs in-process; verified by the standalone
   boot smoke instead.
4. **`utils/logger.js`** console-only branches (dev-only paths).

## Production bugs found and fixed during this phase

Fixed in the single permitted dedicated commit `cfd36c9`:

1. `middleware/auth.js` — malformed percent-encoded Cookie header threw
   URIError → 500 on any request carrying it (availability).
2. `utils/fileStore.js` — corruption repair never triggered because
   detection matched the legacy "Unexpected token" message text;
   now uses `err instanceof SyntaxError` (Node-version-proof).
