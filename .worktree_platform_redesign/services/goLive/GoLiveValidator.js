(function(root){
  'use strict';const ns=root.OmniGoLive=root.OmniGoLive||{};
  function profile(value){const errors=[];if(!value||value.customerNumber!=='001')errors.push('CUSTOMER_NUMBER_INVALID');if(!value||!/^[_a-z0-9]+$/.test(value.tenantId||''))errors.push('TENANT_ID_INVALID');if(!value||!/^[a-z0-9]+(?:-[a-z0-9]+)*$/.test(value.workspaceSlug||''))errors.push('WORKSPACE_SLUG_INVALID');if(!value||!value.businessName||!value.businessNameAr)errors.push('BUSINESS_NAME_REQUIRED');if(!value||!/^01\d{9}$/.test(value.phone||''))errors.push('PHONE_INVALID');if(!value||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(value.email||''))errors.push('EMAIL_INVALID');if(!value||!Array.isArray(value.categories)||value.categories.length!==5)errors.push('CATEGORIES_INVALID');return Object.freeze({valid:errors.length===0,errors:Object.freeze(errors)});}
  function readiness(input){input=input||{};const checks=Object.freeze({profile:profile(input.profile).valid,productionMode:Boolean(input.productionMode&&input.productionMode.enabled),ownerAuthenticated:Boolean(input.productionMode&&input.productionMode.ownerAuthenticated),serverEnabled:Boolean(input.productionMode&&input.productionMode.serverEnabled),supabaseConnection:Boolean(input.connection&&input.connection.valid),installerReady:Boolean(input.installerReady),rollbackReady:Boolean(input.rollbackReady)});const failed=Object.entries(checks).filter(([,v])=>!v).map(([k])=>k);return Object.freeze({valid:failed.length===0,checks,failed:Object.freeze(failed),score:Math.round((Object.keys(checks).length-failed.length)/Object.keys(checks).length*100),dangerousActionsEnabled:failed.length===0});}
  ns.GoLiveValidator=Object.freeze({version:'1.0.0',profile,readiness});
})(typeof globalThis!=='undefined'?globalThis:window);
