(function(root){
  'use strict';const ns=root.OmniPerformanceScale=root.OmniPerformanceScale||{};
  function analyze(input){input=input||{};const queries=(input.queries||[]).map(x=>Object.freeze({...x,slow:Number(x.durationMs||0)>=Number(input.slowThresholdMs||500)})),tables=(input.tables||[]).map(x=>Object.freeze({...x,large:Number(x.rows||0)>=Number(input.largeTableRows||100000)}));const recommendations=[];queries.filter(x=>x.slow&&x.filterColumns).forEach(x=>recommendations.push({type:'index',table:x.table,columns:x.filterColumns,sqlGenerated:false}));tables.filter(x=>x.large).forEach(x=>recommendations.push({type:'pagination',table:x.name,rows:x.rows}));return Object.freeze({slowQueries:Object.freeze(queries.filter(x=>x.slow)),largeTables:Object.freeze(tables.filter(x=>x.large)),indexRecommendations:Object.freeze(recommendations),storageEstimateBytes:tables.reduce((s,x)=>s+Number(x.estimatedBytes||0),0),connectionPoolPreview:Object.freeze({min:1,max:Number(input.poolMax||10),connectionsOpened:0}),repositoryProfile:Object.freeze(input.repositoryProfile||{}),sqlExecuted:0,readOnly:true});}
  ns.DatabasePerformanceAnalyzer=Object.freeze({version:'1.0.0',analyze});
})(typeof globalThis!=='undefined'?globalThis:window);
